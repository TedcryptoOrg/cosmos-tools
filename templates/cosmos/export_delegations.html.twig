{% extends 'base.html.twig' %}


{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-sm-12 col-md-10">
                <h1>Export delegations</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-12 col-md-10">
                {{ form_start(form) }}
                    {{ form_rest(form) }}
                   <div class="form-group pull-right">
                       <button type="submit" class="btn btn-primary">Request export</button>
                   </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts_footer %}

{% endblock %}